version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: parking_db
    environment:
      MYSQL_DATABASE: parking_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
